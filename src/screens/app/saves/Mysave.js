import {Box, Text} from '../../../theme';

const MySaves = () => {
  return (
    <Box backgroundColor={'mainwhite'} flex={1} padding="xl">
      <Text color={'mainblack'}>My Saves</Text>
    </Box>
  );
};

export default MySaves;
